// find buttons with projects
// push button => appears in new block "filter-projects", projects filtered
// push button in "filter-project" => button removed, projects filtered

const skills__items = document.querySelectorAll(".skills__item");
const skills__button = document.querySelectorAll(".skills__button");
